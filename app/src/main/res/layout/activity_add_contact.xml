<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:layout_marginTop="10dp"
    android:padding="16dp"
    tools:context=".AddContactActivity">

    <!-- Header text -->
    <TextView
        android:id="@+id/headerText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        android:gravity="center"
        android:text="Enter the Details"
        android:textColor="#3F51B5"
        android:textSize="22sp"
        android:textStyle="bold" />

    <!-- Name input with rounded corners and shadow -->
    <EditText
        android:id="@+id/nameInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:background="@drawable/rounded_edittext"
        android:elevation="4dp"
        android:hint="Name"
        android:inputType="textPersonName"
        android:padding="14dp"
        android:textColor="#000000"
        android:textSize="16sp" />

    <!-- Email input with rounded corners and shadow -->
    <EditText
        android:id="@+id/emailInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:background="@drawable/rounded_edittext"
        android:elevation="4dp"
        android:hint="Email"
        android:inputType="textEmailAddress"
        android:padding="14dp"
        android:textColor="#000000"
        android:textSize="16sp" />

    <!-- Phone number input with rounded corners and shadow -->
    <EditText
        android:id="@+id/phoneInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        android:background="@drawable/rounded_edittext"
        android:elevation="4dp"
        android:hint="Phone Number"
        android:inputType="phone"
        android:padding="14dp"
        android:textColor="#000000"
        android:textSize="16sp" />

    <TextView
        android:id="@+id/tag_ID_TV"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        />


    <!-- Submit button with rounded corners and shadow -->
    <Button
        android:id="@+id/submitButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@drawable/rounded_button"
        android:backgroundTint="#512525"
        android:elevation="6dp"
        android:padding="12dp"
        android:text="Submit"
        android:textColor="#070404"
        android:textSize="16sp" />

</LinearLayout>
